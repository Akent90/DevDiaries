<h1>Welcome to Dev Diaries!</h1>
<p>Find the latest posts from developers around the world.</p>

{{#if posts}}
    <div class="post-container">
        {{#each posts as |post|}}
            <div class="post">
                <h2><a href="/posts/{{post.id}}">{{post.title}}</a></h2>
                <p>Date Created: {{post.createdAt}}</p>
            </div>
        {{/each}}
    </div>
{{else}}
    <p>No posts found!</p>
{{/if}}